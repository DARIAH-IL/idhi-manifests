[project]
name = "idhi-manifests"
version = "0.1.0"
description = "Israeli Digital Humanities Index — manifests and tooling"
requires-python = ">=3.10"
dependencies = [
    "linkml>=1.8",
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.7",
    "ruamel-yaml>=0.19.1",
]

[tool.uv]
package = false
