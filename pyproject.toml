[project]
name = "idhi-manifests"
version = "0.1.0"
description = "Israeli Digital Humanities Index — manifests and tooling"
requires-python = ">=3.10"
dependencies = [
    "linkml>=1.8",
    "ruamel-yaml>=0.19.1",
    "zensical>=0.0.52",
]

[tool.uv]
package = false
